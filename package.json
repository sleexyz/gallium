{
    "private": true,
    "workspaces": [
        "gallium-lang"
    ],
    "devDependencies": {
        "flow-bin": "^0.55.0",
        "flow-typed": "^2.2.3",
        "lerna": "^2.5.1"
    },
    "scripts": {
      "build": "lerna run build",
      "check-all": "flow && yarn format-check && yarn jest",
      "format": "lerna run format",
      "format-check": "lerna run format-check",
      "jest": "lerna run jest"
    }
}
